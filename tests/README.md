# Tests

Backend tests in `backend/` use Vitest with Supertest for HTTP integration.

```
npm test
```

Frontend tests are not included in this iteration; the `frontend/` directory
is a stub for future component-level coverage.
